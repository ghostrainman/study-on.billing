nelmio_api_doc:
    documentation:
        info:
            title: 'API документация'
            description: 'Документация к API study-on.billing'
            version: '1.0.0'
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: [ ]
    areas:
        path_patterns:
            - ^/api
    models:
        use_jms: true